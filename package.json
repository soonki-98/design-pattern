{
  "name": "singleton",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --force",
    "singleton": "yarn build && node ./examples/singleton/singleton.js",
    "factory": "yarn build && node ./examples/factory/factory.js"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
